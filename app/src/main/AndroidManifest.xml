<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.omelchenkoaleks.starttechopolice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="._002_layout.layout_constraintBaseline_toBaselineOfActivity"></activity>
        <activity android:name="._002_layout.layout_constraintHorizontal_biasActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="._001_introduction.MainActivity" />
        <activity
            android:name="._001_introduction.TestActivity"
            android:theme="@style/AppTheme.Floating" />
        <activity android:name="._001_introduction.SecondMainActivity" />
        <activity android:name="._002_layout.layout_constraintWidth_defaultActivity" />
    </application>

</manifest>